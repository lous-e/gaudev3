{
  "name": "bidmesh-negotiate",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --pool=threads",
    "demo": "powershell -NoProfile -Command \"tsc --outDir .demo-dist; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node .demo-dist/src/demo.js; $code = $LASTEXITCODE; Remove-Item -Recurse -Force .demo-dist -ErrorAction SilentlyContinue; exit $code\""
  },
  "dependencies": {
    "express": "^5.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.10.1",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
